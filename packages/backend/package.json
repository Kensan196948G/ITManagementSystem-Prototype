{
  "name": "backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "../../venv/bin/python app.py",
    "dev": "python -m flask --app app.py --debug run --host 0.0.0.0 --port 5000",
    "test": "jest --coverage",
    "test:python": "python -m pytest",
    "lint": "python -m flake8 .",
    "format": "python -m black ."
  },
  "dependencies": {},
  "devDependencies": {
    "jest": "^29.5.0"
  }
}