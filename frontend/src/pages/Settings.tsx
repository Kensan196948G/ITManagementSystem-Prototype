import React from 'react';

const Settings = () => {
    return (
        <div>
            <h1>Settings</h1>
            <p>設定ページのコンテンツをここに追加します。</p>
        </div>
    );
};

export default Settings;